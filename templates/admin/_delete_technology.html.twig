<form method="post" action="{{ path('admin_delete_technology', {'id': technology.id}) }}" onsubmit="return confirm('Es-tu sur de vouloir suprmier cette technologie')">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ technology.id) }}">
    <button class="btn-flat black-text"><i class="material-icons">close</i></button>
</form>
